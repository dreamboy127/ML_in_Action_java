the java code of Machine Learning in Action 

git usage:

    ssh-keygen -t rsa -C "dreamboy127@gmail.com"

    copy ~/.ssh/id_rsa.pub
    ssh -T git@github.com

1 git config --global user.name "Your Real Name"
2 git config --global user.email "you@email.address"
    git init <project>
    cd project
    git add .
    git remote add origin git@github.com:dreamboy127/ML_in_Action_java.git
    git commit -m "first commit"
    git push origin master
